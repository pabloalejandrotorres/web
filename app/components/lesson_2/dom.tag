<web-dom>
  <section>
    <h1>document</h1>
  </section>
  <section>
    <p>En nuestro entorno global (dentro de window), tenemos un elemento llamado `document`.</p>
    <p>Hace referencia al elemento raiz del documento.</p>
    <p>Nos provee funcionalidad global en el documento, como obtener la url actual, crear elementos en el dom.</p>
    <p>Lo podemos acceder de `window.document` o simplemente `document`</p>
  </section>
</web-dom>
