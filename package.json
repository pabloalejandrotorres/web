{
  "name": "web",
  "version": "0.0.1",
  "private": true,
  "description": "slides",
  "engines": {
    "node": ">=0.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "gulp serve",
    "install-dependencies": "bower install"
  },
  "author": "Pablo Torres",
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.7.9",
    "browser-sync": "^2.12.7",
    "gulp": "^3.9.1",
    "gulp-load-plugins": "^1.2.2"
  }
}
